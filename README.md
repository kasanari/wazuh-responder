# Readme

## Install

Run `uv tool install .`.

## How to run

To shut down, run:
`wazuh-responder --command isolate --agent [AGENT NAME]`.

To isolate, run:
`wazuh-responder --command isolate --agent [AGENT NAME]`.
